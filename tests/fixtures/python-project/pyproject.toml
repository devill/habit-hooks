[project]
name = "habit-hooks-python-fixture"
version = "0.0.0"
dependencies = ["requests"]

[tool.deptry]
# `requests` is declared but never imported -> deptry DEP002 (unused-dependency).

[tool.ruff.lint.mccabe]
max-complexity = 5

